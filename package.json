{
  "name": "@aweb/world",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "ready": "npm install pnpm@8 rimraf@latest -g && npm run bootstrap",
    "bootstrap": "pnpm install",
    "clean": "pnpm --recursive exec -- npx rimraf node_modules && npx rimraf node_modules",
    "prepare": "husky install"
  },
  "peerDependencies": {
    "pnpm": "^8.11.0",
    "rimraf": "3.0.2"
  },
  "devDependencies": {
    "@types/wicg-file-system-access": "",
    "husky": "^8.0.3",
    "execa": "^8.0.1",
    "axios": "^1.6.2",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">= 18"
  },
  "pnpm": {}
}
